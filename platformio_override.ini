[env:heltec-v2_0]
; Conservative upload speed for reliable flashing on various cables/adapters
upload_speed = 115200
; Optional: specify port if auto-detect is unreliable
; upload_port = /dev/cu.usbserial-0001

[env:heltec-v2_1]
; Conservative upload speed for reliable flashing on various cables/adapters  
upload_speed = 115200
; Optional: specify port if auto-detect is unreliable
; upload_port = /dev/cu.usbserial-0001
